name: Deploy Web App to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6' # Using a known stable Flutter version
          channel: 'stable'

      - name: Clean project and cache
        run: |
          flutter clean || true
          flutter pub cache clean
          rm -rf .dart_tool || true
          rm -rf .pub-cache || true
          rm -rf build || true

      - name: Get dependencies
        run: flutter pub get

      - name: Build Web App
        run: flutter build web --base-href /subkiller/

      - name: Upload artifact for GitHub Pages
        uses: actions/upload-artifact@v4 # <<< CORRECTED TO v4
        with:
          path: ./build/web

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
