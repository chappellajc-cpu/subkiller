name: subkiller
description: "Subscription tracker and canceler app"
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Or whatever the latest stable version is on pub.dev

flutter:
  uses-material-design: true
